name = "vertex-streetwear"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist/client"

# Environment variables should be set via Wrangler CLI, CI/CD, or Cloudflare Dashboard
# Never commit sensitive values to version control
# Example: wrangler secret put PUBLIC_STOREFRONT_API_TOKEN
[vars]
# PUBLIC_STOREFRONT_API_TOKEN = "" # Set via: wrangler secret put PUBLIC_STOREFRONT_API_TOKEN
# PUBLIC_STORE_DOMAIN = "" # Set via environment
# PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID = "" # Set via: wrangler secret put PUBLIC_CUSTOMER_ACCOUNT_API_CLIENT_ID
# PUBLIC_CUSTOMER_ACCOUNT_API_URL = "" # Set via environment
# SHOP_ID = "" # Set via environment
# PUBLIC_CHECKOUT_DOMAIN = "" # Set via environment
